<section class="privat-page privat-page--narrow" style="text-align:center;padding-top:60px;padding-bottom:60px">
  <div class="privat-empty__heart" style="margin-bottom:8px">♡</div>
  <h1 class="privat-page__title">{{ 'general.404.title' | t }}</h1>
  <p class="privat-form-note" style="max-width:30ch;margin:0 auto 20px">{{ 'general.404.text' | t }}</p>
  <a href="{{ routes.root_url }}" class="privat-btn">{{ 'general.404.button' | t }}</a>
</section>
